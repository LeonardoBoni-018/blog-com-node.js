<div class="container py-5">
  <div class="p-5 mb-4 bg-light rounded-3">
    <div class="container-fluid py-5">
      <h1 class="display-5 fw-bold">Bem vindo ao Blog do Node</h1>
      <p class="col-md-8 fs-4">Esta é uma introdução ao seu conteúdo. Você pode ajustar o layout como preferir usando os utilitários do Bootstrap.</p>
      <button class="btn btn-primary btn-lg" type="button">Crie uma conta</button>
    </div>
  </div>
</div>

<hr>

<h2>
    Postagens recentes:
</h2>

{{!-- Listar as postagens --}}

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}">
            <button class="btn btn-success">Leia mais</button>
        </a>
        <hr>
         <small>Categoria: {{categoria.nome}}</small> <br>
        <small>Data de publicação: {{data}}</small>
        <hr>
    </div>
</div>

{{else}}

    <h5>Nenhuma postagem recente!</h5>

{{/each}}
